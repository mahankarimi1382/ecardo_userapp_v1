## 1.0.51+51 — Close open security/architecture tasks

- Biometric unlock prefers bearer token (no stored password on new logins)
- clearLoggedInUserPassword after login / successful token unlock
- Legacy password path kept one-shot then cleared
- Safe amount parse on add_money controller + withdraw review
- Dead invoice route constants removed
- P2pBinding for MyAds / MyOrder / PaymentAccount shell

Agent: external
