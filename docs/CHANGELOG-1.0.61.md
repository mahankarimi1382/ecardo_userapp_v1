## 1.0.61+61

- Fix CI: SettingsService.saveBonusPopUpShow is instance method (not static)
- Custom DashboardSliverHeaderDelegate (max~120/min~56, easeOutCubic opacity)
- floatHeaderSlivers + RefreshIndicator retained
- OfflineQueueBanner only on navigation/profile/settings routes
- Badge isLabelVisible only when unread > 0
- Splash: probe user endpoint; expired session dialog + ورود مجدد

Agent: external
