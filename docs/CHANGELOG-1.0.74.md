## 1.0.74+74 — empty/error states (client-only P2)

- QR: empty/error view with retry when SVG data missing or load fails
- Beneficiary create/update: use Binding via Get.find (no duplicate Get.put)
- Dynamic password: empty state when HomeController / account number unavailable
- No backend dependency

Agent: server-user
